# Issue #134 Implementation Plan

**Goal:** `docs/candidate-inventory.json` の `static_classification=runtime_dependent` / `status=unresolved` 候補を逆コンパイル根拠で再分類し、Issue #134 で実際に解決できる静的サイトだけを翻訳データへ反映する。

**Architecture:** 候補は「site 単位」ではなく「producer family 単位」で扱う。`EmitMessage` 系は `ui-messagelog-leaf.ja.json` と `messages.ja.json`、`StartReplace()` 系は `templates-variable.ja.json`、blueprint part field は既存 `BlueprintTemplateTranslationPatch` 前提で `BlueprintTemplates/templates.ja.json` を使う。既存 worktree にあるカバー分は再実装せず、`candidate-inventory` の status 同期だけ行う。

**Evidence:** [`docs/candidate-inventory.json`](docs/candidate-inventory.json), [`docs/phase3d-remaining-audit.md`](docs/phase3d-remaining-audit.md), [`docs/phase3-investigation-report.md`](docs/phase3-investigation-report.md), decompiled source under `（ローカル逆コンパイルソース）

## Extracted Runtime-Dependent / Unresolved Sites

### static_resolvable

| Site | Concrete text / source | Action |
| --- | --- | --- |
| `XRL.World.Parts/ShevaStarshipControl.cs:199` | `Exodus launch sequence initiated!`, `Exodus launch in 10...`, `Launching in {Timer}...` from [`ShevaStarshipControl.cs:130-142`](（ローカル逆コンパイルソース）) | 既存 `ui-phase3d-endings.ja.json` の2件を維持し、可変カウントダウンだけ `messages.ja.json` に regex 追加 |
| `Qud.UI/Notification.cs:120` | `Achievement progress.`, `Achievement unlocked!` from [`AchievementInfo.cs:91-100`](（ローカル逆コンパイルソース）) | 現 worktree の [`achievements.ja.json`](Mods/QudJP/Localization/Dictionaries/achievements.ja.json) で既存カバー。status 同期のみ |
| `XRL.World.Effects/Ill.cs:71` | default + caller override 3件 from [`Ill.cs:11`](（ローカル逆コンパイルソース）), [`CardiacArrest.cs:125`](（ローカル逆コンパイルソース）), [`RitualSifrahTokenEffectIll.cs:48`](（ローカル逆コンパイルソース）) | `ui-messagelog-leaf.ja.json` に exact leaf 追加 |
| `XRL.World.Parts.Mutation/IrritableGenome.cs:75` | `Your/The ... irritable genome acts up...` from [`IrritableGenome.cs:75`](（ローカル逆コンパイルソース）) | 現 worktree の [`messages.ja.json`](Mods/QudJP/Localization/Dictionaries/messages.ja.json) で既存カバー。status 同期のみ |

### needs_xml_data

| Site / family | Concrete text / source | Action |
| --- | --- | --- |
| `XRL.Liquids/BaseLiquid.cs:415` | `SlipperyMessage` primary path | `templates-variable.ja.json` に liquid class 5件の variable template を追加 |
| `XRL.Liquids/BaseLiquid.cs:440` | `SlipperyMessage` sticky path | 同上 |
| `XRL.World.Parts/DesalinationPellet.cs:217` | site 本体は `MakeUnderstood(out Message)` で runtime。静的に拾えるのは `Message` / `DestroyMessage` / `ConvertMessage` from [`DesalinationPellet.cs:14-18`](（ローカル逆コンパイルソース）) | 現 worktree の [`templates.ja.json`](Mods/QudJP/Localization/BlueprintTemplates/templates.ja.json) で part field 側は既存カバー。site `:217` 自体は runtime のため translated へは上げない |

### covered_by_current_patch

| Site family | Reason |
| --- | --- |
| `Qud.UI/*` sink-fed rows: `HighScoresRow.cs:79`, `MapScrollerPinItem.cs:12,13`, `Qud.UI/AbilityManagerScreen.cs:471,495`, `CharacterAttributeLine.cs:95`, `CharacterEffectLine.cs:88`, `CyberneticsTerminalRow.cs:83,101`, `GameSummaryScreen.cs:130,131`, `JournalStatusScreen.cs:298,317`, `LeftSideCategory.cs:20,24,28,32`, `LoadingStatusWindow.cs:45`, `Notification.cs:120`, `PlayerStatusBar.cs:505`, `TinkeringDetailsLine.cs:116,126,182`, `TradeScreen.cs:957`, `TutorialManager.cs:261,531,737`, `CategoryIconScroller.cs:16`, `CategoryMenuController.cs:20`, `CategoryMenusScroller.cs:53,54`, `FrameworkHeader.cs:20`, `FrameworkScroller.cs:333`, `HorizontalScroller.cs:78`, `SummaryBlockControl.cs:30`, `ObjectFinderLine.cs:63,74`, `TitledIconButton.cs:23` | current sink patch / exact dictionary coverage。Issue #135 側で扱う系統なので本Issueでは no-op |

### needs_runtime/static_partial

| Site family | Reason |
| --- | --- |
| `XRL.World.Parts/Chat.cs:182,191,206` | `Say` field の runtime split |
| `XRL.World.Parts/Garbage.cs:148,168` | journal note text + direction runtime composition |
| `XRL.World.Parts/LiquidVolume.cs:3538` | liquid collection grammar / container names runtime composition |
| `XRL.World.Parts/MissileWeapon.cs:2472,2490` | event caller-generated message |
| `XRL.World.Parts/Physics.cs:3780` | event placeholder expansion |
| `XRL.World.Parts/Stomach.cs:538` | drinking logic StringBuilder |
| `XRL.World/GameObject.cs:14597` | `Die(..., Message)` caller param |
| `XRL.World.Parts/DesalinationPellet.cs:217` | `Examiner.GetIdentifyMessage()` runtime output; part field templatesとは別 |
| `XRL.World.Parts/Reconstitution.cs:262`, `Spawner.cs:89`, `SwapOnUse.cs:34` | [`docs/phase3d-remaining-audit.md:18-31`](docs/phase3d-remaining-audit.md#L18) にある通り v1.0.4 では XML override 不在の unreachable field。Issue #136 側の除外メモとして維持 |

## Implementation Steps

1. `DoesVerbFamilyTests.cs` と `StartReplaceTranslationPatchTests.cs` に failing tests を追加する。
2. `ui-messagelog-leaf.ja.json` に `Ill` 3文言を追加する。
3. `messages.ja.json` に `^Launching in (\\d+)\\.\\.\\.$` を追加する。
4. `templates-variable.ja.json` に `LiquidInk/Slime/Oil/Water/Gel` の `SlipperyMessage` 5件を追加する。
5. `candidate-inventory.json` では `BaseLiquid:415/440`, `Ill:71`, `IrritableGenome:75`, `ShevaStarshipControl:199`, `Notification:120` を `translated` へ同期し、`DesalinationPellet:217` は runtime のため据え置く。
6. L1 テストと JSON 妥当性確認を実行する。
7. コミット `feat(translation): static_resolvable + needs_xml_data サイト翻訳 (#134)` を作成し、push を試行する。
